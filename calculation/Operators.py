from enum import Enum


class Operators(Enum):
    ADDITION = "+"
    SUBSTRACTION = "-"
    MULTIPLICATION = "x"
    DIVISION = "/"
    FRACTION = "fraction"
    MULTIPLE = "multiple"
    UNITIES = "unities"
